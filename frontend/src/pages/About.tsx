const About = () => {
    return (
        <div>
            This is just a random route to make the navbar look less empty lol, I might write some
            documentation here later though.
        </div>
    );
};

export default About;
